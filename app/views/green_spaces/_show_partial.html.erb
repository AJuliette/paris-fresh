<h2 class="centered-text">Ilots de fraîcheur - Espaces verts "frais"</h2>

<div class="container">
    <div class="flex-container">
        <% green_spaces.each do |green_space| %>
            <div class="card">
                <div class="card-content-title-only">
                    <p class="emoji">🌲🌲🌲</p>
                    <h3><%= green_space.name %></h3>
                    <div class="subtitle"><%= green_space.address %></div>
                </div>
            </div>
        <% end %>
    </div>
</div>
